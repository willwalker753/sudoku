{
  "private": true,
  "name": "sudoku",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "build:gh": "GITHUB_PAGES=sudoku preact build && dot-json ./build/manifest.json start_url \"/sudoku/\"",
    "dev": "preact watch"
  },
  "devDependencies": {
    "dot-json": "^1.2.2",
    "preact-cli": "^3.0.0"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}
